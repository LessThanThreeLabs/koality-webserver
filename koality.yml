--- #Koality: 0.1.5
languages:
  nodejs: 0.8.12
setup:
  - packages:
    - system:
      - fontconfig  # for phantomjs
    - npm:
      - iced-coffee-script
      - grunt-cli
      - karma
      - npm install
  - scripts:
    - path: scripts
      script: ./install_phantomjs.sh
test:
  scripts:
    - unit tests:
        script:
          - npm test
